# 简立得 · 待做清单（PRD vs 当前进度）

> 对照 PRD v1.0，标注各功能点的实现状态。
> 状态: ✅ 已完成 / 🔶 部分完成 / ❌ 未开始 / ⏸ P2 暂不做

---

## 一、P0 必做功能（MVP 核心）

### 用户系统
| 功能 | 状态 | 说明 |
|------|------|------|
| 用户注册/登录（手机号+密码） | ✅ | 后端 auth 路由 + 前端 Header 登录弹窗已完成 |
| JWT Token 鉴权 | ✅ | `utils/security.py` 实现 |

### 简历上传与解析
| 功能 | 状态 | 说明 |
|------|------|------|
| PDF 上传（5MB 限制） | ✅ | `UploadPage.tsx` + 后端 `resume.py` 路由 |
| PDF 文本解析（pdfplumber） | ✅ | `services/document_parser.py` |
| Word (DOCX) 解析支持 | ✅ | `python-docx` 库 + `document_parser.py` 统一处理 |

### AI 诊断
| 功能 | 状态 | 说明 |
|------|------|------|
| 四大维度评分（ATS/内容/项目/匹配） | ✅ | `ai_service.py` + `diagnosis_service.py` |
| 免费版预览（总分 + 前2条问题） | ✅ | `DiagnosisPage.tsx` 免费预览区 |
| 完整诊断报告（付费解锁） | ✅ | 支付后 `is_unlocked=1`，返回完整 `detail` |
| Mock 支付（¥9.90） | ✅ | `payment_service.py` `mock_pay()` |
| **四维雷达图可视化** | ✅ | SVG 雷达图，免费版模糊化处理 |
| **ATS 关键词覆盖率检测** | ✅ | 已覆盖/缺失关键词分色展示 |
| **STAR 法则专项评估** | ✅ | 进度条 + 百分比展示 |
| **数据量化密度统计** | ✅ | 进度条 + 百分比展示 |
| **优先级改进清单（跨维度排序）** | ✅ | 按严重程度排序的改进清单 |
| **会员自动解锁诊断** | ✅ | 会员跳过付费，诊断报告直接解锁 |

### AI 润色
| 功能 | 状态 | 说明 |
|------|------|------|
| 整体润色版本生成 | ✅ | `polish_service.py` + `PolishPage.tsx` |
| 逐段对比（原文 vs 润色版） | ✅ | diff 视图，`PolishPage.tsx` |
| Mock 支付（¥19.90） | ✅ | |
| **会员直接使用润色** | ✅ | 会员无需付费，直接创建润色 |
| **AI 润色导出 PDF / TXT** | ❌ | 前端仅有「复制全文」，无 PDF/TXT 导出 |
| **接受/拒绝每条修改** | ❌ | 当前仅展示对比，不能选择性地接受/拒绝 |

### 历史记录
| 功能 | 状态 | 说明 |
|------|------|------|
| 历史记录页面 | ✅ | `/history` 路由，显示所有诊断报告及评分 |
| 简历列表（最近 N 份） | ✅ | 同上 |
| 历史打分总览 | ✅ | 四维分数 + 总评分 + 等级 |
| 润色状态标注 | ✅ | "已润色"徽章 |

### 个人中心
| 功能 | 状态 | 说明 |
|------|------|------|
| 个人中心页面 | ✅ | `/profile` 路由 |
| 会员状态展示 | ✅ | Header 会员徽章 + ProfilePage 状态卡片 |
| **Mock 激活会员（点击即生效）** | ✅ | `POST /activate-member`，无需支付 |
| 支付记录 | ✅ | 订单列表展示 |
| **真实支付对接（微信支付）** | ❌ | 待申请微信支付 API 后接入 |

### 平台适配
| 功能 | 状态 | 说明 |
|------|------|------|
| **iOS App Store 上架** | ❌ | 当前为 Web 应用（React），PRD 原计划用 Flutter 做原生 App |
| **Android APK 分发** | ❌ | 同上 |
| 移动端响应式适配 | 🔶 | 使用 Tailwind 响应式类，但未系统测试移动端体验 |
| 微信内部分发测试 | ❌ | |

---

## 二、P1 可延期功能（2-3 周迭代）

| 功能 | 状态 | 说明 |
|------|------|------|
| **JD 匹配分析独立页面** | 🔶 | 上传页有 `target_job` 输入框，AI 会据此调整评分；缺少独立的匹配分析页面、关键词热力图、匹配度可视化 |
| **AI 润色导出 PDF** | ❌ | 需要后端生成 PDF（如 WeasyPrint / ReportLab）或前端用 `jsPDF` |
| **版本对比功能** | ❌ | 需新增对比页面，实现两份诊断报告并排展示 |
| **苹果内购 StoreKit** | ❌ | 当前只有 Mock 支付，需对接 App Store Server Notifications |
| **微信支付真实对接** | ❌ | 需对接微信支付 v3 API，替换当前 Mock 实现 |
| **推送通知** | ❌ | 诊断/润色完成后通知用户 |
| **分享引导** | ❌ | 完成后引导分享到微信/朋友圈 |

---

## 三、P2 暂不做功能

| 功能 | 状态 |
|------|------|
| 月订阅会员体系 | ⏸ |
| 多语言支持（英文简历） | ⏸ |
| 岗位库 / 职位推荐 | ⏸ |
| 社区功能（互评、晒分） | ⏸ |
| 简历在线编辑器 | ⏸ |
| HR 端 / B 端功能 | ⏸ |

---

## 四、PRD 功能架构对照（缺失模块）

对照 PRD 第四章「产品功能架构」：

| 模块 | 子功能 | 状态 |
|------|--------|------|
| **首页** | 上传简历入口 | ✅ |
| | 扫码上传（手机端快速入口） | ❌ |
| | 历史记录入口 | ✅ |
| | 会员状态展示 | ✅ |
| **简历诊断** | 基础评分总览 | ✅ |
| | 四维雷达图 | ✅ |
| | Top3 核心问题（免费2条+加锁1条） | ✅ |
| | ATS 检测关键词列表 | ✅ |
| | 格式问题 | ✅ 在 AI 返回中展示 |
| | STAR 完整度 | ✅ |
| | 量化数据密度 | ✅ |
| | 项目经历逐条点评 | ✅ 依赖 AI 返回 |
| | 技能关键词覆盖率 | ✅ |
| | 改进清单优先级排序 | ✅ |
| **岗位匹配** | 粘贴 JD 文本 | 🔶 在上传页内嵌，无独立匹配页面 |
| | 关键词热力图 | ❌ |
| | 匹配度评分 | 🔶 在诊断评分中体现，未独立展示 |
| **历史记录** | 所有上传记录 | ✅ |
| | 版本标注 | ❌ |
| | 版本对比（付费） | ❌ |
| **个人中心** | 会员状态 & 已解锁记录 | ✅ |
| | 会员激活（Mock） | ✅ |
| | 支付记录 | ✅ |
| | 帮助 & 反馈 | ❌ |
| | 设置 | ❌ |

---

## 五、技术债务 / 建议优化项

| 项目 | 说明 |
|------|------|
| AI 结果缓存 (Redis) | PRD 第 9.3 节要求 MD5 缓存 + 7 天有效期，当前未实现 |
| AI 输出 JSON 校验 | `ai_service.py` 中 `_validate_json` 有基本校验，建议加强字段完整性和评分合理性校验 |
| 免费诊断次数限制 | ✅ 已实现（`check_free_limit`），会员跳过限制 |
| 错误处理完善 | 全局异常处理器已配置，但各路由的特定错误码需补充 |
| 日志系统 | 当前仅用了 `logging` 基础配置，建议接入结构化日志 |
| 数据库索引 | `created_at` 字段未建索引，历史记录查询可能慢 |
| 前端状态管理 | 当前用 localStorage + fetch 直调，规模增大后建议引入状态管理库 |

---

## 六、最近更新（2026-06-06）

1. ✅ Word (.docx) 解析支持（后端 `python-docx` + 前端上传支持）
2. ✅ Mock 会员激活（"成为会员"按钮，点击即生效，无需支付）
3. ✅ 会员权限粒度：会员自动解锁诊断报告、免费使用 AI 润色
4. ✅ Header 自动从 `/auth/me` 刷新会员状态
5. ⏳ 微信支付 API 待申请后接入
